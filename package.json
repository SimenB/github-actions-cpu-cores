{
  "name": "github-actions-cpu-cores",
  "version": "3.0.0",
  "main": "index.mjs",
  "license": "MIT",
  "scripts": {
    "preversion": "yarn build && git add dist/",
    "build": "ncc build index.mjs"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0",
    "prettier": "^3.0.0"
  },
  "dependencies": {
    "@actions/core": "^3.0.0"
  }
}
